# EVIDENCE：ui-design（experimental 试点）

状态：Tier0（候选）。审美/体验型技能，**无法纯自动打分**——采用"自动（规范符合）+ 人工（美感评审）"双裁判；当前仅完成 L0，人工评审待做。

## L0 静态自检

- [x] frontmatter description 可判定触发（"设计/美化界面、别像模板"）
- [x] 步骤 = 动作 + 检查项；含"验证"小节
- [x] 绑定项目规范（FRONTEND/tokens/FSD）而不是空谈审美
- [x] 无工具绑定；来源标注（frontend-design 思路，MIT）
- [x] 体量小（约 40 行）

## 基准方案（未跑，待定）

- **自动部分（可脚本化）**：检查产物样式是否只用 tokens/规范类、无内联魔法值；可访问性（对比度/语义）可交给 axe 类工具 → 仍属"规范符合"，非美感。
- **人工部分（必要）**：同一页面任务，"无技能 vs 加载技能"各产一版截图 → 由作者（或他人）A/B 评审美感与一致性。
- **任务**：起一个 mini 页面任务（如"表单卡片 + 列表 + 空态"，固定文案），放 `benchmarks/tasks/` 待补。

## 结论

L0 通过；自动规范检查与人工美感 A/B **待执行**——按 `benchmarks/AB.md` 协议补真实模型 + 人评后，再评估是否提升内置。
